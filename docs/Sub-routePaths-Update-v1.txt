[DIRECTIVE: FEATURE]
TASK TITLE: Change 'Scanned Routes Directory Path' from showing '/sub-route-1' and showing the actual subroute like /about or /contact-us
PRIORITY: High

---

DESCRIPTION OF REQUESTED CHANGE:
1. The Scanned Routes Directory PAth shows /sub-route-1, /sub-route-2 and so on in a long list.  It is of no use to a user when it comes to fixing the page.  Replace /sub-route-1 references with the actual names of the pages or the folders so it is more informative.  Providing a whole URL there with a button to go directly to the page is also useful if it is not too much effort.  Also replace the heading that says 'Route' to say 'Page paths'.  
2. Next to 'Word Count' include a '?' button with a info popup which would help understand what the relevance of wordcount(based on the AEO_USER_CAPABILITIES.md file and AEO_TOOL_MAPPING_V1.xlx file.)  Provide a color coding where the wordcount is too higher, too low or ideal, and mention it in the help popup.
3. For 'Canonical_URL' include a '?' button with an info popup which outlines the important of the canonical URL.  Also instead of saying 'Active', it would be good to provide the actual canoninical URL mentioned.  If there is no Canonical URL mentioned, highlight that  in the list in the appropriate row.
4. For 'Structure' include a '?' button with an info popup that mentions that 1H1 is what is ideal, and if any H2s, H3s, H4s, etc do not follow heirarchy, then that has to be highlighted.  For the line items that say '1 H1/4 H2' and so on, include a tick or cross mark which shows clearly if the structure has been followed or not.  I expect this will come from the analyzer.

EXPECTED PIPELINE EXECUTION:
1. Stage 1: Print plain-English BDD test cases and file modification list, then HALT for my 'APPROVED' signal.
2. Stage 2 & 3: Implement code and run automated Vitest passes.
3. Stage 4: Sync AEO_CHANGELOG.md, AEO_REGRESSION_MATRIX.md, and AEO_MASTER_PROJECT_PLAN.md.
4. Stage 5 & 6: Verify integrity and execute Git commit + push to remote 'dev' branch.

Begin Stage 1 Pre-Flight Analysis now and await my approval.